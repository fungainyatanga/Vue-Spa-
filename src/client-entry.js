import { app } from './app'

app.$mount('#app')

// Tells hot module to start the hot module propagation
// and accept that everything is reloaded correctly
//if (module.hot) {
  //module.hot.accept()
//}